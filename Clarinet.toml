[project]
name = "yield-vault"
description = "Time-locked yield vault with real-time interest accrual using Clarity 4"
authors = ["Stacks Builder"]
telemetry = false
cache_dir = "./.cache"
requirements = []

[contracts.yield-vault]
path = "contracts/yield-vault.clar"
clarity_version = 4
epoch = 3.3

[repl.analysis]
passes = ["check_checker"]
check_checker = { trusted_sender = false, trusted_caller = false, callee_filter = false }
